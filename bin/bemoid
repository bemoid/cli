#!/usr/bin/env node

const program = require('commander')

program
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('scaffold', 'Scaffold boilerplate from a template')
  .parse(process.argv)